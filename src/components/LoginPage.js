import React from 'react'
import LoginForm from './LoginForm'

const LoginPage = (props) => {

    
    return (
        <div>
            <LoginForm {...props} />
        </div>
    )
}

export default LoginPage
