
let initialState = {
    lists: [],
    isDone: false,
    isPinned: false,
}

export const reducer = (prevState = initialState, action) => {
    switch (action.type) {
        
        default:
            return { ...prevState }
    }
}


